---
layout: ../layouts/SimpleLayout.astro
title: Meet the Board
subtitle:
centered: true
max_w: max-w-6xl
---

import Profile from "../components/Profile.astro";
import { Image } from 'astro:assets';
import { executive, management, team } from "../images/profiles";

# Executive Board

<div class="flex flex-wrap justify-center justify-items-center gap-x-16 gap-y-8">
  {executive.map((member) => (
    <Profile 
      image={member.image} 
      href={member.href}
      quality={90}
      imageClass="w-64 h-64 object-cover shadow-lg hover:shadow-xl transition-all duration-200"
    >
      <h1 class="mt-4 text-xl font-semibold">{member.name}</h1>
      <p class="text-lg text-gray-700">{member.role}</p>
    </Profile>
  ))}
</div>

# Management Heads
<div class="flex flex-wrap justify-center justify-items-center gap-x-16 gap-y-8">
  {management.map((member) => (
    <Profile 
      image={member.image} 
      href={member.href}
      quality={90}
      imageClass="w-64 h-64 object-cover shadow-lg hover:shadow-xl transition-all duration-200"
    >
      <h1 class="mt-4 text-xl font-semibold">{member.name}</h1>
      <p class="text-lg text-gray-700">{member.role}</p>
    </Profile>
  ))}
</div>

# Team Heads

<div class="flex flex-wrap justify-center justify-items-center gap-x-16 gap-y-8">
  {team.map((member) => (
    <Profile 
      image={member.image} 
      href={member.href}
      quality={90}
      imageClass="w-64 h-64 object-cover shadow-lg hover:shadow-xl transition-all duration-200"
    >
      <h1 class="mt-4 text-xl font-semibold">{member.name}</h1>
      <p class="text-lg text-gray-700">{member.role}</p>
    </Profile>
  ))}
</div>
